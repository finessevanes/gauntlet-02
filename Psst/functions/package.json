{
  "name": "psst-functions",
  "version": "1.0.0",
  "description": "Cloud Functions for Psst messaging app",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "npm run build && firebase deploy --only functions",
    "deploy:staging": "npm run build && firebase use staging && firebase deploy --only functions",
    "deploy:prod": "npm run build && firebase use production && firebase deploy --only functions",
    "logs": "firebase functions:log",
    "clear-db": "ts-node scripts/clearFirestore.ts"
  },
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "@pinecone-database/pinecone": "^6.1.2",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.1.0",
    "googleapis": "^164.1.0",
    "luxon": "^3.7.2",
    "openai": "^6.6.0"
  },
  "devDependencies": {
    "@types/luxon": "^3.7.1",
    "@types/node": "^24.9.1",
    "firebase-functions-test": "^3.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "private": true
}
