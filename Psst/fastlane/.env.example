# ============================================================================
# FASTLANE ENVIRONMENT VARIABLES - SETUP INSTRUCTIONS
# ============================================================================
#
# HOW TO USE THIS FILE:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your actual values (see instructions below)
# 3. NEVER commit .env to git (it contains secrets!)
#
# ============================================================================

# App Store Connect API Authentication
# =====================================
# Create your API key at: https://appstoreconnect.apple.com/access/api
# Navigate to: Users and Access → Keys → App Store Connect API → Click "+"

# The Key ID from App Store Connect (e.g., "ABC123XYZ")
APP_STORE_CONNECT_API_KEY_ID="YOUR_KEY_ID_HERE"

# The Issuer ID from App Store Connect (e.g., "12345678-90ab-cdef-1234-567890abcdef")
APP_STORE_CONNECT_ISSUER_ID="YOUR_ISSUER_ID_HERE"

# Path to the .p8 private key file (relative to project root)
APP_STORE_CONNECT_API_KEY_FILEPATH="./fastlane/AuthKey_YOUR_KEY_ID_HERE.p8"

# Match Certificate Management
# =============================
# Private GitHub repository URL for storing certificates (create this first!)
MATCH_GIT_URL="git@github.com:YOUR_USERNAME/psst-certificates.git"

# Password to encrypt the certificate repository (choose a strong password)
MATCH_PASSWORD="YOUR_STRONG_PASSWORD_HERE"
