{
  "rules": {
    "presence": {
      "$uid": {
        ".read": true,
        ".write": "$uid === auth.uid"
      }
    },
    "typing": {
      "$chatID": {
        ".read": "auth != null",
        "$uid": {
          ".write": "$uid === auth.uid"
        }
      }
    }
  }
}

